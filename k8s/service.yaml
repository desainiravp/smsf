apiVersion: v1
kind: Service
metadata:
  name: smsf-service
  namespace: smsf
spec:
  type: NodePort
  selector:
    app: smsf-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30007   # Range: 30000–32767
